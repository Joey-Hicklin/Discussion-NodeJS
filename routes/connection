var sql = require('mysql');

var connect = sql.createConnection({
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'discussion'
});

module.exports = {
    sql:sql,
    connect:connect
};